-- models/employees/employees.sql

SELECT
    id,
    first_name,
    last_name,
    department_id,
    salary,
    hire_date
FROM
    employees  -- Replace with the actual source table name in your database
